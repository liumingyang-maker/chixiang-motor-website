# Chixiang Motor 事实确认回写报告

日期：2026-07-30
阶段：Phase 6.1A Fact Calibration  
审核方式：网站所有者单人确认

## 输入结果

- 公司事实：25 条
- 发动机规格及家族记录：21 条
- 合计：46 条
- `保留当前值`：40 条（其中 35 条由用户确认数字 `28` 代表该选择）
- `修改，以备注为准`：5 条
- `暂不确定`：1 条
- `不公开`：0 条

所有记录通过唯一“确认项目”标签匹配回稳定 `fact_id` 或 `spec_id`，46 个 ID 全部唯一，没有按行号猜测。

## 回写状态

- `APPROVED_PUBLIC`：45 条
- `APPROVED_INTERNAL_ONLY`：0 条
- `PENDING_REVIEW`：1 条

`APPROVED_PUBLIC` 记录已填写候选主值、`Site Owner`、批准日期和所有者确认来源。发动机型号的空白字段继续表示未知，不能因为整行获批而对外推断。

## 七项明确修改

1. 成立时间拆分为两个事实：自 2003 年起从事摩托车发动机行业；当前公司于 2007 年注册。
2. 员工数量确认为 100 名。
3. 唯一批准 Logo 为 `https://chixiangmotor.com/images/logo.webp`；不存在的 `logo.png` 引用列入后续页面清理。
4. 水冷型号批准为 `CG150SB`；共享拉美数据中的 `CG150B` 记录为录入错误。
5. `Hanwei`、`HW Water` 和 `CG Heavy` 确认为同一家族，通常使用 `HW Water`；该家族无内置倒挡。CG water-cooled 家族可选内置倒挡，但不作为主要卖点。
6. ISO 9001 与 CCC 调整为允许公开通用存在性声明，但不得补写或推断尚未提供的证书版本、编号、主体、范围、适用型号或有效期。
7. HW Water 全部型号统一采用 1.5 L 机油容量、18 级磁电机和 20 滚子离合器，且无内置倒挡；“18 级”为网站所有者指定术语，不改写成“18 极”，英文技术名称待确认。该确认仅适用于家族级配置，不据此补写型号级排量、缸径行程、安装尺寸或性能数值。

## 一致性处理

- 当前注册地址和工厂地址采用含谷镇地址；Jiuli Village 记录为搬迁前旧址。
- 公司短介绍和长介绍保留原有产品与能力结构，但成立时间统一改为“2003 年行业经验、2007 年公司注册”，不再使用“公司成立于 2003 年”。
- CG150SB、CG175SB、CG200SB 和 CG250SB 的候选配置统一为单缸、四冲程、水冷、立式；倒挡表述统一为按型号和订单确认的可选内置倒挡配置。
- 四条原 `FAMILY_INTAKE` 记录已按网站所有者补充确认，提升为有边界的 `FAMILY` 公开事实：CG 平衡轴、海啸水冷、HW Water 和自动离合水冷系列。它们可以用于家族定位和共用配置，但不能发布成具体型号规格，也不能用于填补未知数值。

## 继续待确认

1. 152FMH：需要确认型号代码与 110/125/130 营销排量、实际排量、缸径行程和启动配置的对应关系。

ISO 9001 与 CCC 已获准使用不含具体证书资料的通用公开声明。ISO 9001 不写标准版本、编号、主体、范围、签发机构或有效期；CCC 只写可提供具有 CCC 认证的产品，不得泛化为全部产品，也不写编号、主体、适用型号或有效期。

## 项目边界

本次只修改事实校准 CSV、审核指南和本报告；没有修改 HTML、CSS、JavaScript、Worker、Schema、Sitemap、robots、广告标签、分析标签或表单逻辑。

Phase 6.1B 可以从 45 条 `APPROVED_PUBLIC` 记录生成逐页变更清单。四条 `FAMILY` 记录只能提供家族级公开表述；1 条 152FMH 待确认记录仍不得进入新的公开页面内容。
