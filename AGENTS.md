# Chixiang Motor Website — 项目规则

## 项目概况

- 这是 Chixiang Motor 的静态多语言 B2B 网站。
- 网站主要使用 HTML、CSS 和原生 JavaScript。
- 代码托管于 GitHub，生产环境托管于 Cloudflare Pages。
- Cloudflare Pages 不使用框架预设和构建命令，发布目录为仓库根目录。
- 联系表单提交至 `/api/contact`。
- 联系表单 Worker 位于 `workers/contact-api`。
- 默认使用简体中文与用户沟通。

## 开始任务前

每次开始任务必须先检查：

1. 当前工作目录和 Git 仓库根目录。
2. `git status --short --branch`。
3. 当前分支。
4. 最近相关提交。
5. 与任务有关的文件、文档和已有实现。
6. 是否存在未提交更改。

不得覆盖、删除或混入用户已有的未提交修改。

## 分支规则

- 禁止直接修改 `main`。
- 每个独立任务使用独立分支。
- 分支从最新、干净且正确的基础分支创建。
- 不得执行 `git reset --hard`、`git clean -fd` 或强制推送。
- 未经用户明确确认，不得 push、创建或合并 PR。
- 未经用户明确确认，不得部署生产环境。

## 实施方式

- 任务明确时直接执行，不重复询问已经提供的信息。
- 修改前先说明简短计划、涉及文件和验证方式。
- 优先查明根因，不通过盲目试错修改代码。
- 只修改完成当前任务所必需的文件。
- 不进行与任务无关的大范围重构、格式化或文件改名。
- 优先复用已有组件、样式、脚本和页面结构。
- 不虚构测试结果、代码行为或已完成状态。

## 多语言内容

- 俄语、西班牙语、英语和其他语言页面保持相互独立。
- 不因修改一个国家页面而批量改写其他国家页面。
- 保留本地化语言表达，不机械逐字翻译。
- 产品型号、技术参数和适配说明必须以已有资料为依据。
- 不虚构市场数据、搜索量、CPC、利润、销量、客户或证据。
- 不修改冻结的研究数据，除非用户明确要求。

## 表单、广告和生产保护

- 联系表单生产接口为 `/api/contact`。
- 测试时优先使用本地模拟或安全测试方式。
- 未经明确确认，不得向生产环境提交真实测试询盘。
- 不得擅自修改 Google Ads、Yandex、分析代码或转化触发逻辑。
- 转化事件只能在确认的成功提交后触发，不能仅因浏览、点击或滚动触发。
- 不得擅自修改 Cloudflare Pages、Worker、DNS、环境变量或生产部署设置。

## 密钥与隐私

- 不读取、展示、记录或提交 API Key、密码、验证码和客户数据。
- 不提交 `.env`、凭据文件、本地缓存或编辑器私有配置。
- Cloudflare Turnstile Site Key 是公开配置；Secret Key 必须只保存在安全环境变量中。
- 发现疑似密钥泄露时立即停止相关操作并提醒用户处理。

## 验证要求

根据任务运行所有适用检查：

- `git diff --check`
- 检查实际改动范围和意外文件
- 本地静态网站预览
- 浏览器控制台错误检查
- 桌面端和移动端布局检查
- 页面内链、导航、按钮和 WhatsApp 链接
- 表单正常输入、空输入和错误输入
- 转化事件是否只在提交成功后触发
- Worker 或脚本已有的测试与 QA 工具

没有实际运行的检查不得标记为通过。

## 完成报告

完成任务后必须说明：

1. 当前分支。
2. 修改的文件。
3. 每个修改的目的。
4. 实际运行的命令和检查。
5. 每项检查的真实结果。
6. 尚未验证的内容。
7. 遗留风险。
8. 建议的提交信息。

## 必须等待确认的操作

以下操作必须先获得用户明确确认：

- commit
- push
- 创建或合并 PR
- 删除文件或分支
- 修改生产配置
- 修改广告账户或转化跟踪
- 部署网站或 Worker
- 向真实外部服务提交数据
